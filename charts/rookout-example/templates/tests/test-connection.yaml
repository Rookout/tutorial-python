apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rookout-example.fullname" . }}-test-connection"
  labels:
{{ include "rookout-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rookout-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
