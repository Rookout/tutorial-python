{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rookout-example.serviceAccountName" . }}
  labels:
{{ include "rookout-example.labels" . | nindent 4 }}
{{- end -}}
