apiVersion: v1
kind: Service
metadata:
  name: {{ include "rookout-example.fullname" . }}
  labels:
    {{- include "rookout-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "rookout-example.selectorLabels" . | nindent 4 }}
