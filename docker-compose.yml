version: '2'
services:
  tutorial-python:
    container_name: pythontodoapp
    restart: always
    build: .
    environment:
      - ROOKOUT_AGENT_HOST=rookout-python-agent
    ports:
      - "5000:5000"
    links:
      - rookout-python-agent
  rookout-python-agent:
    container_name: rookout-python-agent
    image: rookout/agent
    environment:
      - ROOKOUT_TOKEN=$ROOKOUT_TOKEN
      - ROOKOUT_AGENT_TAGS=pytut
    ports:
      - "7486:7486"
